interface:
  display_name: "Movies Nerd"
  short_description: "One-command movie and series downloads"
  default_prompt: "Use $movies-nerd to download and organize these titles through its single foreground controller, with automatic failover, cleanup, and collection-aware recommendations."
